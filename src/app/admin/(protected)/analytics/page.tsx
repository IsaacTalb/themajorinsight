import { EmptyState, PageHeader } from "@/components/admin/AdminShell";
import { getPosts } from "@/lib/admin-data";
export default async function AnalyticsPage(){const posts=await getPosts();const viewed=posts.filter(p=>p.view_count>0);return <main className="admin-main"><PageHeader eyebrow="Performance" title="Analytics" description="First-party view totals recorded for published insights."/>{viewed.length?<div className="admin-table-wrap"><table className="admin-table"><thead><tr><th>Insight</th><th>Views</th></tr></thead><tbody>{viewed.sort((a,b)=>b.view_count-a.view_count).map(p=><tr key={p.id}><td>{p.title}</td><td>{p.view_count.toLocaleString()}</td></tr>)}</tbody></table></div>:<EmptyState title="No analytics available" detail="Nothing is estimated or fabricated. Recorded views will appear here."/>}</main>}
