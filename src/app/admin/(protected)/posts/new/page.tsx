import { PageHeader } from "@/components/admin/AdminShell";
import { requireAdmin } from "@/lib/admin-auth";
export default async function NewPostPage() { await requireAdmin(["owner","admin","editor","author"]); return <main className="admin-main"><PageHeader eyebrow="Content / Posts" title="New post" description="Set up the story record. The full article editor arrives in a later phase."/><section className="admin-panel admin-form"><label>Working title<input placeholder="Untitled insight" disabled /></label><div className="admin-form-row"><label>Status<select disabled><option>Draft</option></select></label><label>Author<select disabled><option>Select author</option></select></label></div><p className="admin-note">Post creation is intentionally read-only in this phase. No incomplete editor or unsafe mutation is exposed.</p></section></main>; }
