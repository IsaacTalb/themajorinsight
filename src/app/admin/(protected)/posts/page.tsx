import Link from "next/link";
import { EmptyState, PageHeader, Status } from "@/components/admin/AdminShell";
import { getPosts } from "@/lib/admin-data";
export default async function PostsPage() { const posts = await getPosts(); return <main className="admin-main"><PageHeader eyebrow="Content" title="Posts" description="Manage every insight across the publishing workflow." action={<Link className="admin-primary" href="/admin/posts/new">New post</Link>}/>{posts.length ? <div className="admin-table-wrap"><table className="admin-table"><thead><tr><th>Title</th><th>Status</th><th>Views</th><th>Last updated</th></tr></thead><tbody>{posts.map(p => <tr key={p.id}><td><Link href={`/admin/posts/${p.id}`}>{p.title}</Link><small>/{p.slug}</small></td><td><Status value={p.status}/></td><td>{p.view_count.toLocaleString()}</td><td>{new Date(p.updated_at).toLocaleDateString()}</td></tr>)}</tbody></table></div> : <EmptyState title="No posts found" detail="Start a new draft to build your editorial library."/>}</main>; }
